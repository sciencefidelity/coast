---
import { Navigation, Settings } from "../lib/interfaces"
import Navbar from "../components/Navbar.astro"

const { navigation, settings } = Astro.props as {
  navigation: Navigation
  settings: Settings
}
---
<header id="gh-head" class="gh-head has-cover">
  <Navbar
    navigation={navigation}
    settings={settings}
  />
</header>
